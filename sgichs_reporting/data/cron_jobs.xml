<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Tarea programada para verificar la conexión de los dispositivos -->
        <record id="ir_cron_ping_devices" model="ir.cron">
            <field name="name">Network: Ping IT Hardware</field>
            <field name="model_id" ref="sgichs_hardware.model_it_asset_hardware"/>
            <field name="state">code</field>
            <field name="code">model.cron_ping_devices()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
        </record>
    </data>
</odoo>